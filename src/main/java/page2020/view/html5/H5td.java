/* @author MILO-GAO(高振銘)@2020 */ package page2020.view.html5;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import page2020.view.Content;import page2020.view.Element;public class H5td extends Element<H5td, Content<?>> {	public static final class Attr {		public static final String colspan = "colspan";		public static final String rowspan = "rowspan";	}	private static Logger log = LoggerFactory.getLogger(H5td.class);	public H5td() {		super("td");	}	public H5td colspan(int number) {		if (number > 0) {			super.getAttrMap().put(Attr.colspan, Integer.valueOf(number));		} else {			log.error("colspan: argument(number) should be more then zero !");		}		return this;	}	public H5td rowspan(int number) {		if (number > 0) {			super.getAttrMap().put(Attr.rowspan, Integer.valueOf(number));		} else {			log.error("rowspan: argument(number) should be more then zero !");		}		return this;	}}