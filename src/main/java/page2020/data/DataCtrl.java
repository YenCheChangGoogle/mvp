/* @author MILO-GAO(高振銘)@2020 */ package page2020.data;import javax.servlet.http.HttpSession;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.PathVariable;import org.springframework.web.bind.annotation.PostMapping;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.ResponseBody;import page2020.core.Log;import page2020.util.EmptyUtil;@Controller@RequestMapping("/data2020")public class DataCtrl {	private static final Logger log = LoggerFactory.getLogger(DataCtrl.class);	@PostMapping("/{uuid}")	public @ResponseBody TableData data(@PathVariable("uuid") String uuid, HttpSession session) {		if (EmptyUtil.not(uuid)) {			Object object = session.getAttribute(uuid);			if (object != null) {				session.removeAttribute(uuid);					TableData result = (TableData) object;				if (Log.data) {					log.info("data: uuid='" + uuid + "', data=" + result.getLength());				}				return result;			}		}		if (Log.data) {			log.info("data: uuid='" + uuid + "', NOT ALLOWED");		}		return new TableData();	}}