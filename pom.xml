<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- 
			其中 2.7.18 是 Spring Boot 2.x 最後一個 OSS 免費版本(2023/11 EOL)。
		    此版本本身無法修補 2024~2026 年才揭露的 Spring Framework / Spring Security 核心 CVE,
		    這些必須靠下方 dependencyManagement 覆寫個別套件版本,或長期規劃升級到 Spring Boot 3.x/4.x 
		-->
		<version>2.7.18</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.fubon</groupId>
	<artifactId>mvp</artifactId>
	<version>1.0</version>
	<packaging>war</packaging>
	<name>mvp</name>
	<description>富邦MVP郵件驗證系統</description>
	<properties>
		<java.version>1.8</java.version>
		<maven.test.skip>true</maven.test.skip>

		<!-- 
			===== Sonatype 掃描報告(2026/07/07)修補用版本控管 =====
		    注意：這些屬性必須實際被下方 <dependencyManagement> 引用才會生效，
		    單純宣告 property 不會覆寫 Spring Boot Parent BOM 管理的版本。
		    升版後務必執行 `mvn dependency:tree -Dverbose` 確認實際生效版本，
		    並完整跑過迴歸測試（Tomcat/Netty/Spring Security 中間有行為變更）。 
		-->
		<log4j2.version>2.25.5</log4j2.version> <!-- 2.24.3 => 2.25.5-->
		<netty.version>4.2.16.Final</netty.version> <!-- 4.1.135.Final => 4.1.136.Final => 4.2.16.Final-->
		<tomcat.version>9.0.120</tomcat.version>
		<bouncycastle.version>1.85</bouncycastle.version>
		<snakeyaml.version>2.2</snakeyaml.version>
		<commons-compress.version>1.27.1</commons-compress.version>
		<commons-text.version>1.13.0</commons-text.version>
		<commons-io.version>2.16.1</commons-io.version>
		<commons-collections4.version>4.5.0</commons-collections4.version>
		<httpcore5.version>5.4.2</httpcore5.version> <!-- 5.3.1 => 5.4.2 -->
		<jetty.version>12.0.33</jetty.version> <!-- 9.4.57.v20241219 => 12.0.33 -->
		<poi.version>5.2.5</poi.version>
		<mysql.version>9.2.0</mysql.version> <!-- 8.0.33 => 9.2.0 -->
		<jackson.version>2.21.5</jackson.version> <!-- 2.17.2 => 2.18.8 => 2.21.5 -->
		<spring-security.version>5.8.13</spring-security.version> <!-- 5.8.13 => 6.3.9 類別已經不同 無法升版 -->
		<spring-data-bom.version>2021.2.18</spring-data-bom.version>
		<xerces.version>2.12.2</xerces.version>
		
		<!-- 
			htmlunit 官方已停止維護 net.sourceforge.htmlunit，主線遷移到 org.htmlunit。
		    4.x 版需要 Java 11+，與目前 java.version=1.8 衝突，需另外評估，見下方註解
		-->
		<htmlunit.version>2.70.0</htmlunit.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- Jackson: CVE-2026-54512/54513/54514/54515/54518 等反序列化漏洞 -->
			<dependency>
				<groupId>com.fasterxml.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>${jackson.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<!-- Netty: CVE-2026-45674/47691(10.0分)、CVE-2026-42581/42584 等一系列 RCE/DoS -->
			<dependency>
				<groupId>io.netty</groupId>
				<artifactId>netty-bom</artifactId>
				<version>${netty.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<!-- Spring Security: CVE-2023-34034(9.8分,認證繞過)、CVE-2026-22732 -->
			<dependency>
				<groupId>org.springframework.security</groupId>
				<artifactId>spring-security-bom</artifactId>
				<version>${spring-security.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<!-- Spring Data: CVE-2026-41695/41716/41711/41721(8.2~8.7分, SpEL注入) -->
			<dependency>
				<groupId>org.springframework.data</groupId>
				<artifactId>spring-data-bom</artifactId>
				<version>${spring-data-bom.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<!-- Tomcat embed: CVE-2025-24813/31651(9.8分)、CVE-2025-66614、CVE-2026-43512 等 -->
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-core</artifactId>
				<version>${tomcat.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-websocket</artifactId>
				<version>${tomcat.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-el</artifactId>
				<version>${tomcat.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-jasper</artifactId>
				<version>${tomcat.version}</version>
			</dependency>

			<!-- SnakeYAML: CVE-2022-1471(9.8分, 反序列化RCE) -->
			<dependency>
				<groupId>org.yaml</groupId>
				<artifactId>snakeyaml</artifactId>
				<version>${snakeyaml.version}</version>
			</dependency>

			<!-- Commons Text: CVE-2022-42889(9.8分, 類Log4Shell的插值注入) -->
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-text</artifactId>
				<version>${commons-text.version}</version>
			</dependency>

			<!-- Commons Compress: CVE-2021-35515/35516/35517/36090 -->
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-compress</artifactId>
				<version>${commons-compress.version}</version>
			</dependency>

			<!-- Commons IO: CVE-2024-47554 -->
			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons-io.version}</version>
			</dependency>

			<!-- Commons Collections 3.x 為停止維護的舊系列，改鎖 4.x 系列避免拉入舊版 -->
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-collections4</artifactId>
				<version>${commons-collections4.version}</version>
			</dependency>

			<!-- HttpCore5: CVE-2026-54399/54428 -->
			<dependency>
				<groupId>org.apache.httpcomponents.core5</groupId>
				<artifactId>httpcore5</artifactId>
				<version>${httpcore5.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.httpcomponents.core5</groupId>
				<artifactId>httpcore5-h2</artifactId>
				<version>${httpcore5.version}</version>
			</dependency>

			<!-- Jetty(由 htmlunit 帶入): CVE-2026-2332(9.1分)、CVE-2025-11143 -->
			<dependency>
				<groupId>org.eclipse.jetty</groupId>
				<artifactId>jetty-http</artifactId>
				<version>${jetty.version}</version>
			</dependency>

			<!-- Apache POI: sonatype-2023-4187(7.5分) -->
			<dependency>
				<groupId>org.apache.poi</groupId>
				<artifactId>poi</artifactId>
				<version>${poi.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.poi</groupId>
				<artifactId>poi-ooxml</artifactId>
				<version>${poi.version}</version>
			</dependency>

			<!-- BouncyCastle: CVE-2025-14813(9.3分)、CVE-2026-5598 -->
			<dependency>
				<groupId>org.bouncycastle</groupId>
				<artifactId>bcprov-jdk18on</artifactId>
				<version>${bouncycastle.version}</version>
			</dependency>

			<!-- 
				xerces: CVE-2017-7503(9.8分, XXE)。注意：2.12.2 已是官方最後版本，
			    上游已無更新，剩餘風險需靠程式面關閉 DTD/外部實體解析來補強，
			    無法單靠升版消除，建議另開 Fortify 風險接受文件追蹤
			-->
			<dependency>
				<groupId>xerces</groupId>
				<artifactId>xercesImpl</artifactId>
				<version>${xerces.version}</version>
			</dependency>

			<!-- MySQL 官方新 groupId，取代已停止維護的 mysql:mysql-connector-java -->
			<dependency>
				<groupId>com.mysql</groupId>
				<artifactId>mysql-connector-j</artifactId>
				<version>${mysql.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-taglibs</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-beans</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.tomcat.embed</groupId>
			<artifactId>tomcat-embed-jasper</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>

		<!-- log4j -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-log4j2</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-api</artifactId>
			<version>${log4j2.version}</version>
			<scope>provided</scope>
			<type>pom</type>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-core</artifactId>
			<version>${log4j2.version}</version>
			<scope>provided</scope>
			<type>pom</type>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-to-slf4j</artifactId>
			<version>${log4j2.version}</version>
			<scope>provided</scope>
			<type>pom</type>
		</dependency>

		<!-- secure -->		
		<dependency> 
			<groupId>org.apache.taglibs</groupId> 
			<artifactId>taglibs-standard-spec</artifactId> 
			<version>1.2.5</version> 
		</dependency> 	
		<dependency> 
			<groupId>org.apache.taglibs</groupId> 
			<artifactId>taglibs-standard-impl</artifactId> 
			<version>1.2.5</version> 
		</dependency> 
		<dependency> 
			<groupId>org.apache.taglibs</groupId> 
			<artifactId>taglibs-standard-jstlel</artifactId> 
			<version>1.2.5</version> 
		</dependency> 
		<dependency>
			<groupId>org.owasp.esapi</groupId>
			<artifactId>esapi</artifactId>
			<version>2.7.0.0</version>
		</dependency>
		<dependency>
			<groupId>org.jsoup</groupId>
			<artifactId>jsoup</artifactId>
			<version>1.18.3</version>
		</dependency>
		<dependency>
			<groupId>com.github.ulisesbocchio</groupId>
			<artifactId>jasypt-spring-boot-starter</artifactId>
			<version>3.0.5</version>
		</dependency>

		<!-- client -->
		<!-- 
			https://mvnrepository.com/artifact/net.sourceforge.htmlunit/htmlunit
		    TODO: net.sourceforge.htmlunit 官方已停止維護，主線已遷移到 org.htmlunit:htmlunit(4.x)，
		    但 4.x 需要 Java 11+，目前專案 java.version=1.8，無法直接換 groupId。
		    這裡先鎖到 2.x 系列最後一版止血，長期仍建議規劃 JDK 升級後改用 org.htmlunit
		-->
		<dependency>
			<groupId>net.sourceforge.htmlunit</groupId>
			<artifactId>htmlunit</artifactId>
			<version>${htmlunit.version}</version>
		</dependency>			
		<dependency>
			<groupId>xalan</groupId>
			<artifactId>xalan</artifactId>
			<version>2.7.3</version>
		</dependency>
		<dependency>
			<groupId>xalan</groupId>
			<artifactId>serializer</artifactId>
			<version>2.7.2</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/dom4j/dom4j -->
		<dependency>
			<groupId>org.dom4j</groupId>
			<artifactId>dom4j</artifactId>
			<version>2.1.4</version>
		</dependency>
		<dependency>
			<groupId>jaxen</groupId>
			<artifactId>jaxen</artifactId>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-web-services -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web-services</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
		</dependency>

		<!-- environment -->					
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-tomcat</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>com.microsoft.sqlserver</groupId>
			<artifactId>mssql-jdbc</artifactId>
			<scope>runtime</scope>

			<!-- 如果跑的是 JDK 17 以上就改成 12.8.1.jre17 -->
			<!-- <version>12.8.1.jre11</version> -->
			
		</dependency>

		<!-- BouncyCastle Provider（AES 解密用），版本由上方 dependencyManagement 統一控管 -->
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcprov-jdk18on</artifactId>
		</dependency>
		<!-- Apache Commons Net（FTP 上傳用） -->
		<dependency>
			<groupId>commons-net</groupId>
			<artifactId>commons-net</artifactId>
			<version>3.11.1</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<!-- Apache POI for Excel parsing (AI result report)，版本由上方 dependencyManagement 統一控管 -->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
			<exclusions>
				<exclusion>
					<groupId>org.junit.vintage</groupId>
					<artifactId>junit-vintage-engine</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
			[測試環境使用MySQL的時候]
			原 mysql:mysql-connector-java:8.0.12 (CVE-2018-3258/2023-22102 等多個高危漏洞)
		    官方 groupId 已改為 com.mysql，版本由上方 dependencyManagement 統一控管(8.0.33)。
		    注意：這是 driver 內部 groupId/artifactId 變更，不是單純版號升級，
		    需確認程式碼中 Class.forName("com.mysql.cj.jdbc.Driver") 等寫法與連線URL相容(通常相容)，
		    並在測試環境完整跑過 DB 連線/交易相關測試
		-->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
		</dependency>

	</dependencies>

	<profiles>
		<!-- Test Profile -->
		<profile>
			<id>test</id>
			<build>
				<resources>
					<resource>
						<directory>src/main/resources</directory>
						<includes>
							<include>application.properties-test</include>
							<include>log4j2.properties</include>
							<include>ESAPI.properties</include>
							<include>esapi-java-logging.properties</include>
							<include>antisamy-esapi.xml</include>
							
							<include>validation.properties</include>
							<include>messages.properties</include>
							<include>messages_en.properties</include>
							<include>messages_zh_TW.properties</include>
							
							<include>paper/email</include>
							
						</includes>
					</resource>
				</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>1.8</version>
						<executions>
							<execution>
								<phase>process-resources</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<!-- 把 application.properties-test 改名成 application.properties -->
										<copy
											file="src/main/resources/application.properties-test"
											tofile="${project.build.outputDirectory}/application.properties"
											overwrite="true" />

										<!-- 刪除輸出目錄下的 application.properties-test -->
										 <delete file="${project.build.outputDirectory}/application.properties-test" />
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

		<!-- Prod Profile -->
		<profile>
			<id>prod</id>
			<build>
				<resources>
					<resource>
						<directory>src/main/resources</directory>
						<includes>
							<include>application.properties-prod</include>
							<include>log4j2.properties</include>
							<include>ESAPI.properties</include>
							<include>esapi-java-logging.properties</include>
							<include>antisamy-esapi.xml</include>
														
							<include>validation.properties</include>
							<include>messages.properties</include>
							<include>messages_en.properties</include>
							<include>messages_zh_TW.properties</include>
							
							<include>paper/email</include>
							
						</includes>
					</resource>
				</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>1.8</version>
						<executions>
							<execution>
								<phase>process-resources</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<!-- 把 application.properties-prod 改名成 application.properties -->
										<copy
											file="src/main/resources/application.properties-prod"
											tofile="${project.build.outputDirectory}/application.properties"
											overwrite="true" />

										<!-- 刪除輸出目錄下的 application.properties-prod -->
										 <delete file="${project.build.outputDirectory}/application.properties-prod" />
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
	
	<build>
		<defaultGoal>compile</defaultGoal>
		<finalName>mvp</finalName>

		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
			    <groupId>org.apache.maven.plugins</groupId>
			    <artifactId>maven-surefire-plugin</artifactId>
			    <configuration>
			        <skipTests>true</skipTests>
			    </configuration>
			</plugin>
			
		</plugins>
	</build>
	
</project>