/* @author MILO-GAO(高振銘)@2020 */ package page2020.unit;import java.util.List;import page2020.core.App;import page2020.view.B4;import page2020.view.C3;import page2020.view.H5;import page2020.view.bs4.B4element;import page2020.view.bs4.B4nav;import page2020.view.bs4.B4text.TextX;import page2020.view.html5.H5a;import page2020.view.html5.H5button;public class Upill extends B4element {	private static final String unit_name = "pill";	public Upill() {		super();		super.name(unit_name);			}	public Upill draw(int active, String uri, List<Long> idList) {		B4nav pill = B4.nav(2);		int size = idList.size();		for (int i=0; i < size; i++) {			H5a link = H5.a().href(uri + "/update/" + idList.get(i)).text(String.valueOf(i + 1));			pill.item(link);			if (i == active) {				pill.active().disabled();			}		}		H5button insertButton = App.button("insert.outline");		H5a a = H5.a();		a.setClassSet(insertButton.getClassSet());		a.setContents(insertButton.getContents());		a.cssClass(TextX.nowrap);		a.css(C3.padding("0.25rem 1rem"), C3.margin("0.125rem"), C3.border.radius("1rem"));		a.href(uri + "/insert");		super.append(			B4.row(true).append(				B4.col().device(9).append(pill),				B4.col().device(3).cssClass(TextX.right).append(a)),			B4.rule(1).mb(2)		);		return this;	}}