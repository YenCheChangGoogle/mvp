/* @author MILO-GAO(高振銘)@2020 */ package page2020.view.html5;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import page2020.view.Content;import page2020.view.Element;import page2020.view.Tag;public class H5dl extends Element<H5dl, Content<?>> {	private static Logger log = LoggerFactory.getLogger(H5dl.class);	public H5dl() {		super("dl");	}	@Override	protected boolean isContent(Content<?> c) {		if (super.isContent(c) && c instanceof Tag<?>) {			Tag<?> tag = (Tag<?>) c;			switch (tag.getTagName()) {			case "dt":			case "dd":				return true;			default:				log.error("dl: only contents of 'dt', 'dd' are valid !");				break;			}		}		return false;	}}