/* @author MILO-GAO(高振銘)@2020 */ package page2020.view.html5;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import page2020.util.EmptyUtil;public class H5video extends H5audio {	public static final class Attr {				public static final String height = "height";		public static final String poster = "poster";				public static final String width = "width";	}	private static Logger log = LoggerFactory.getLogger(H5video.class);	public H5video() {		super("video");	}	public H5video height(String pixels) {		if (EmptyUtil.not(pixels)) {			super.getAttrMap().put(Attr.height, pixels.trim());		} else {			log.error("height: argument(pixels) is empty !");		}		return this;	}	public H5video poster(String url) {		if (EmptyUtil.not(url)) {			super.getAttrMap().put(Attr.poster, url.trim());		} else {			log.error("poster: argument(url) is empty !");		}		return this;	}	public H5video width(String pixels) {		if (EmptyUtil.not(pixels)) {			super.getAttrMap().put(Attr.width, pixels.trim());		} else {			log.error("width: argument(pixels) is empty !");		}		return this;	}	@Override	protected boolean isAttr(String attrName) {		switch (super.attrName(attrName)) {		case Attr.height:		case Attr.poster:		case Attr.width:			return true;		default:			return super.isAttr(attrName);		}	}}