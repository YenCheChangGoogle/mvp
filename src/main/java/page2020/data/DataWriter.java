/* @author MILO-GAO(高振銘)@2020 */ package page2020.data;import java.util.ArrayList;import java.util.List;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.scheduling.annotation.Scheduled;import org.springframework.stereotype.Service;@Servicepublic class DataWriter {	private static Object lock = new Object();	private static List<TableData> pool = new ArrayList<TableData>();	public static void write(TableData source) {		synchronized (lock) {			pool.add(source);		}	}	@Autowired DataAsync async;	@Scheduled(fixedRate=1000)	public void scheduled() {		List<TableData> buffer = new ArrayList<TableData>();		synchronized (lock) {			while (! pool.isEmpty()) {				buffer.add(pool.remove(0));			}		}		while (! buffer.isEmpty()) {			async.write(buffer.remove(0));		}	}}