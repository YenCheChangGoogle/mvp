
# 🔍 MVC310003 / 310004 / 310005 三大查詢 API 完整分析報告

---

## 一、MVC310003 — 查詢主檔明細（單筆）

### 1. 功能定位

**用途：** 依 UUID 查詢單一 EmailMaster 申請單的完整資訊。

### 2. 上行電文必填欄位

| 欄位 | 用途 | 必填 |
|------|------|------|
| `QUERY_UUID` | **真正的查詢條件** — 要查的 UUID | ✅ |
| `UUID` | 格式驗證用（不可空） | ✅ |
| `BRANCH` | 格式驗證用 | ✅ |
| `TELLER_NO` | 格式驗證用 | ✅ |
| `CUST_ID` | 格式驗證用 | ✅ |
| `ID_TYPE` | 格式驗證用 | ✅ |

> ⚠️ 注意：雖然 `UUID` 欄位必填，但它只用於**輸入格式檢查**，實際查詢條件是 **`QUERY_UUID`**。

### 3. 業務邏輯流程

```
收到XML → invalid310003() 檢查6個欄位不可空 → 
    ↓ valid=true
依 QUERY_UUID 查 EMAILMAS → 
    ↓ entity != null AND status != '99' ?
    ↓ YES → database=true → 寫影像檔記錄 → 回傳完整資料
    ↓ NO  → database=false  → 回傳錯誤碼 9999
```

### 4. 等效 SQL

```sql
-- 核心查詢（EmailMasterRepo.findOneByUuid）
SELECT * FROM EMAILMAS WHERE UUID = ? AND STATUS <> '99'

-- 實際上是 JPA method name derivation：
-- findOneByUuid(String uuid)
-- → SELECT * FROM EMAILMAS WHERE UUID = :uuid
-- 然後在 Java 層再檢查 status != '99'
```

### 5. 回傳欄位（成功時）

| 回應欄位 | 來源欄位 | 說明 |
|----------|---------|------|
| MESSAGE | - | 訊息文字 |
| MESSAGE_CODE | - | 0000=成功 |
| TX_DATE, TX_TIME | - | 當前系統時間 |
| BRANCH | EMAILMAS.BRANCH | 分行代碼 |
| TELLER | EMAILMAS.TELLER | 櫃員編號 |
| PREV_EMAIL_ADDR | EMAILMAS.PREV_EMAIL_ADDR | 原 Email |
| AFTER_EMAIL_ADDR | EMAILMAS.AFTER_EMAIL_ADDR | 新 Email |
| CHG_DATE, CHG_TIME | EMAILMAS.CHG_DATE/TIME | 變更日期/時間 |
| CHNL_NAME | EMAILMAS.CHNL | 通路（**注意：回應欄位名為 CHNL_NAME 但實際填的是 CHNL**） |
| REASON | EMAILMAS.REASON | 原因代碼 |
| ONLINE | EMAILMAS.ON_OFF_LINE | 線上/離線 |
| STATUS | EMAILMAS.STATUS | 主狀態 |
| TX_STATUS | EMAILMAS.TX_STATUS | 交易狀態 |
| DESCRIPTION | - | 錯誤描述（依 ERR_CODE 查詢） |
| ERR_CODE | EMAILMAS.ERR_CODE | 錯誤碼 |
| REMARK | EMAILMAS.REMAKR | 備註 |

### 6. 輸入驗證邏輯 (`invalid310003()`)

```java
// 以下任一欄位為空 → 返回 true（不通過）
return EmptyUtil.is(uuid, branch, teller, idNo, idType, queryUuid);
```

---

## 二、MVC310004 — EMAILDTL 明細查詢（狀態追蹤）

### 1. 功能定位

**用途：** 依 UUID 查詢該申請單的所有**狀態變更紀錄**（EmailDetail 表）。每一筆 EmailMaster 可能有多个 EmailDetail 記錄，代表該申請單經歷的每個狀態轉換。

### 2. 上行電文必填欄位

| 欄位 | 用途 | 必填 |
|------|------|------|
| `QUERY_UUID` | **查詢條件** — 要查的 UUID | ✅ |

> 其他欄位在格式上不需檢查（程式碼只 check QUERY_UUID）。

### 3. 業務邏輯流程

```
收到XML → 解析 QUERY_UUID → 
    ↓ QUERY_UUID != empty ?
    ↓ YES → valid=true
查詢 EMAILDTL WHERE UUID = QUERY_UUID → 
    ↓ details.size() > 0 ?
    ↓ YES → database=true → 回傳所有明細（TxRepeat列表）
    ↓ NO  → database=false  → 回傳錯誤碼 9999
```

### 4. 等效 SQL

```sql
-- 核心查詢（EmailDetailRepo.findAllByUuid）
SELECT * FROM EMAILDTL WHERE UUID = ?

-- JPA method name derivation：
-- findAllByUuid(String uuid)
-- → SELECT * FROM EMAILDTL WHERE UUID = :uuid
```

### 5. 回傳欄位（成功時 — TxRepeat 列表）

每個 `TxRepeat` 區塊代表一次狀態變更：

| 回應欄位 | 來源欄位 | 說明 |
|----------|---------|------|
| MESSAGE, MESSAGE_CODE, TX_DATE, TX_TIME | - | 基本回應頭 |
| **TxRepeat** (重複區塊) | — | 每次狀態变更记录 |
| └ UUID | EMAILDTL.UUID | 關聯的主檔 UUID |
| └ CHG_DATE, CHG_TIME | EMAILDTL.CHG_DATE/TIME | 該次變更的日期/時間 |
| └ RESP_DATE, RESP_TIME | EMAILDTL.RESP_DATE/TIME | 回應日期/時間 |
| └ TX_STATUS | EMAILDTL.TX_STATUS | 該次狀態碼 |
| └ ERR_CODE | EMAILDTL.ERR_CODE | 錯誤碼 |
| └ DESCRIPTION | - | 錯誤描述（依 ERR_CODE 查詢） |

### 6. TX_STATUS 狀態碼對照

| TX_STATUS | 說明 |
|-----------|------|
| 01 | 收到申請 |
| 11 | 寄信後 |
| 12 | 客戶點擊連結 |
| 13 | 客戶收到（已讀信但尚未確認） |
| 99 | 作廢 |

---

## 三、MVC310005 — AI 外撥情形報表

### 1. 功能定位

**用途：** 查詢所有與 AI 外撥相關的申請單紀錄（FLAG 欄位不為空），支援日期範圍過濾和分頁。

### 2. 上行電文欄位

| 欄位 | 用途 | 必填 |
|------|------|------|
| `QUERY_UUID` | 指定查單一筆（可選，不傳則查全部） | ❌ |
| `FROM_DATE` | 起始日期 YYYYMMDD（可選） | ❌ |
| `TO_DATE` | 結束日期 YYYYMMDD（可選） | ❌ |
| `NEXT_KEY` | 分頁游標（可選，空白=從頭開始） | ❌ |

### 3. 業務邏輯流程

```
收到XML → 解析 QUERY_UUID / FROM_DATE / TO_DATE / NEXT_KEY → 
    ↓
QUERY_UUID 有傳入 ?
    ↓ YES (單筆模式)
查 EMAILMAS WHERE UUID = QUERY_UUID AND STATUS <> '99'
→ 放入 entities 列表(1筆)

    ↓ NO (全部模式)
查 EMAILMAS WHERE STATUS='00' AND TX_STATUS IN ('11','13')
→ 記憶體過濾 FLAG != NULL
→ 若 FROM_DATE/TO_DATE 有值，再按 CHG_DATE 做範圍過濾
    ↓
entities.size() > 0 ?
    ↓ YES
分頁計算 (每頁30筆，NEXT_KEY為游標)
→ 回傳 TxRepeat 列表（不足30筆補空白）
    ↓ NO
回傳錯誤碼 9999
```

### 4. 等效 SQL

**模式 A — QUERY_UUID 指定時：**

```sql
SELECT * FROM EMAILMAS WHERE UUID = ? AND STATUS <> '99'
```

**模式 B — 全量查詢時（核心）：**

```sql
-- 第一步：取得候選清單（EmailDao.overdueResend()）
SELECT * FROM EMAILMAS 
WHERE STATUS = '00' 
  AND TX_STATUS IN ('11', '13') 
ORDER BY ID ASC

-- 第二步：Java 記憶體過濾 FLAG IS NOT NULL AND FLAG <> ''
-- 第三步：若有日期範圍，在 Java 做 CHG_DATE 過濾
-- WHERE CHG_DATE >= FROM_DATE AND CHG_DATE <= TO_DATE
```

> ⚠️ **重要發現：** 程式碼中有宣告 `list01`（查 STATUS=01 的紀錄）但**從未使用**。目前實際只查 `STATUS='00'` 的處理中記錄。如果已完成(STATUS=01)的 FLAG 標記記錄也需要報表，這是個 **Bug / TODO**。

### 5. 分頁邏輯

| NEXT_KEY | beginIdx | endIdx | 說明 |
|----------|----------|--------|------|
| 空/未傳入 | 0 | min(30, total) | 第一頁 |
| "30" | 30 | min(60, total) | 第二頁 |
| "60" | 60 | min(90, total) | 第三頁 |

- HRETRN = `"E"` → 結束（最後一頁）
- HRETRN = `"C"` → 尚有更多資料
- 每頁固定回傳 **30筆**，不足時用空白 TxRepeat 填補

### 6. FLAG 欄位定義

| FLAG | 說明 |
|------|------|
| `1` | 待處理（需產生 AI 外撥名單） |
| `2` | 已獲取（已加入外撥名單並送 FTP） |
| `0` | 不處理 |

### 7. 回傳欄位（TxRepeat 列表）

| 回應欄位 | 來源欄位 | 說明 |
|----------|---------|------|
| HRETRN | - | E=結束 / C=尚有 |
| MESSAGE, MESSAGE_CODE, TX_DATE, TX_TIME | - | 基本回應頭 |
| NEXT_KEY | - | 下一頁游標 |
| **TxRepeat** (最多30筆) | — | AI外撥報表單筆 |
| └ UUID | EMAILMAS.UUID | 申請單UUID |
| └ CUST_ID | EMAILMAS.ID | 客戶身分證號 |
| └ CUST_NAME | EMAILMAS.CH_NAME | 客戶姓名 |
| └ TEL_NO | EMAILMAS.TEL_NO | AI外撥手機號碼 |
| └ FLAG | EMAILMAS.FLAG | AI外撥標記 |
| └ STATUS | EMAILMAS.STATUS | 主狀態 |
| └ TX_STATUS | EMAILMAS.TX_STATUS | 交易狀態 |
| └ ERR_CODE | EMAILMAS.ERR_CODE | 錯誤碼 |
| └ DESCRIPTION | - | 錯誤描述 |
| └ CHG_DATE, CHG_TIME | EMAILMAS.CHG_DATE/TIME | 變更日期/時間 |

---

## 四、三個 API 比較對照表

| 項目 | 310003 | 310004 | 310005 |
|------|--------|--------|--------|
| **查詢表格** | EMAILMAS | EMAILDTL | EMAILMAS |
| **查詢條件** | QUERY_UUID (單筆) | QUERY_UUID (多筆明細) | QUERY_UUID(可選) + 日期範圍 |
| **回傳類型** | 單一紀錄完整資料 | 同一 UUID 的所有狀態变更记录 | AI外撥報表（分頁列表） |
| **前端用途** | 查看某申請單詳情 | 追蹤某申請單的狀態演變 | 管理員查看AI外撥狀況 |
| **有無分頁** | ❌ | ❌ | ✅ (每頁30筆) |
| **關鍵過濾** | STATUS≠'99' | UUID 匹配 | STATUS='00', TX_STATUS IN(11,13), FLAG IS NOT NULL |

---

## 五、測試方法

### 前置條件

確認服務已啟動：`http://localhost:8080/mvp`

### 測試工具

**推薦：** curl 或 Postman / VS Code REST Client

所有測試均為 `POST http://localhost:8080/mvp/api/XXXXXX`，Content-Type: `text/xml` 或 `application/xml`

---

### 🔹 Test 1：310003 — 成功查詢（先查一筆已知的 UUID）

**Step 1：先用 Hello API 拿一筆 UUID**
```bash
# 先生成一筆資料並拿到 UUID
curl -s http://localhost:8080/mvp/api/hello/110001
```

從回傳 XML 中找到 `<UUID>...</UUID>` 值，或用：
```bash
# 獲取最後生成的 UUID
curl -s http://localhost:8080/mvp/api/hello/getuuid
```

**Step 2：用該 UUID 查詢明細**
```xml
POST http://localhost:8080/mvp/api/310003
Content-Type: text/xml

<?xml version="1.0" encoding="UTF-8"?>
<Tx>
  <TxHead>
    <HTXTID>MVC310003</HTXTID>
  </TxHead>
  <TxBody>
    <UUID>0Z89xxxxxx...</UUID>
    <BRANCH>00200</BRANCH>
    <TELLER_NO>89014100</TELLER_NO>
    <TX_CODE>310003</TX_CODE>
    <CHNL>0Z</CHNL>
    <SUB_CHNL>19</SUB_CHNL>
    <CUST_ID>A123456789</CUST_ID>
    <ID_TYPE>01</ID_TYPE>
    <CUST_NAME/>
    <ENG_NAME/>
    <PREV_EMAIL_ADDR/>
    <AFTER_EMAIL_ADDR/>
    <FROM_DATE/><TO_DATE/><REASON>01</REASON><REMARK/>
    <ON_OFF_LINE>Y</ON_OFF_LINE>
    <QUERY_UUID>替換成你要查的UUID</QUERY_UUID>
    <NEXT_KEY/>
  </TxBody>
</Tx>
```

**期望回應：** MESSAGE_CODE=0000，包含 BRANCH、PREV_EMAIL_ADDR、AFTER_EMAIL_ADDR、STATUS、TX_STATUS 等欄位。

---

### 🔹 Test 2：310003 — QUERY_UUID 不存在

將 `QUERY_UUID` 改成一個不存在的值（如 `NOTEXIST0000000000000000000`）

**期望回應：** MESSAGE_CODE=9999，STATUS=99, TX_STATUS=99

---

### 🔹 Test 3：310003 — 輸入格式錯誤（漏填必填欄位）

```xml
<TxBody>
    <QUERY_UUID>某個UUID</QUERY_UUID>
    <!-- 故意漏掉 UUID、BRANCH 等必填欄位 -->
</TxBody>
```

**期望回應：** MESSAGE_CODE=9999，訊息「輸入格式有誤」

---

### 🔹 Test 4：310004 — 成功查詢明細紀錄

```xml
POST http://localhost:8080/mvp/api/310004
Content-Type: text/xml

<?xml version="1.0" encoding="UTF-8"?>
<Tx>
  <TxHead><HTXTID>MVC310004</HTXTID></TxHead>
  <TxBody>
    <QUERY_UUID>替換成已知的UUID</QUERY_UUID>
  </TxBody>
</Tx>
```

**期望回應：** MESSAGE_CODE=0000，包含多個 `<TxRepeat>` 區塊，每個區塊有 CHG_DATE、CHG_TIME、TX_STATUS 等。

---

### 🔹 Test 5：310004 — QUERY_UUID 為空

```xml
<TxBody>
    <QUERY_UUID/>
</TxBody>
```

**期望回應：** MESSAGE_CODE=9999（輸入格式有誤）

---

### 🔹 Test 6：310005 — 查詢全部 AI 外撥報表（第一頁）

```xml
POST http://localhost:8080/mvp/api/310005
Content-Type: text/xml

<?xml version="1.0" encoding="UTF-8"?>
<Tx>
  <TxHead><HTXTID>MVC310005</HTXTID></TxHead>
  <TxBody>
    <QUERY_UUID/>
    <FROM_DATE/>
    <TO_DATE/>
    <NEXT_KEY/>
  </TxBody>
</Tx>
```

**期望回應：** HRETRN=E 或 C，最多30筆 `<TxRepeat>`，有 NEXT_KEY 指向下一頁。

---

### 🔹 Test 7：310005 — 帶日期範圍查詢

```xml
<TxBody>
    <QUERY_UUID/>
    <FROM_DATE>20260601</FROM_DATE>
    <TO_DATE>20260617</TO_DATE>
    <NEXT_KEY/>
</TxBody>
```

**期望回應：** 只回傳 CHG_DATE 落在 20260601~20260617 之間的 FLAG 不為空的紀錄。

---

### 🔹 Test 8：310005 — 指定單一 UUID 查詢

```xml
<TxBody>
    <QUERY_UUID>替換成已知的UUID</QUERY_UUID>
    <FROM_DATE/>
    <TO_DATE/>
    <NEXT_KEY/>
</TxBody>
```

**期望回應：** 最多1筆 TxRepeat，即該 UUID 對應的紀錄。

---

### 🔹 Test 9：310005 — 分頁測試（第二頁）

先用 Test 6 拿到第一頁回傳的 `NEXT_KEY` 值（如 `"30"`），再發：

```xml
<TxBody>
    <QUERY_UUID/>
    <FROM_DATE/>
    <TO_DATE/>
    <NEXT_KEY>30</NEXT_KEY>
</TxBody>
```

**期望回應：** 從第31筆開始的回傳。

---

## 六、已知問題與建議

| # | 發現 | 嚴重度 | 說明 |
|---|------|--------|------|
| 1 | **310005 只查 STATUS='00'** | 🔴 高 | 程式碼註解說要查 STATUS IN (00,01)，但 `list01` 宣告後未使用，已完成的成功記錄不會被回傳 |
| 2 | **310003 CHNL_NAME 欄位名稱誤導** | 🟡 中 | 回應 XML 中的 `<CHNL_NAME>` 實際填入的是 `entity.getChannel()`（通路代碼），不是通路名稱 |
| 3 | **310005 全量查詢時無 ORDER BY 保證** | 🟡 中 | `overdueResend()` 回傳順序是 `ORDER BY ID`，但分頁依賴記憶體索引，若同時有寫入操作可能導致資料遺漏或重複 |
| 4 | **310005 日期過濾在記憶體做** | 🟢 低 | FROM_DATE/TO_DATE 的過濾是在 Java 層做而非 SQL，如果紀錄量大會影響效能 |
| 5 | **310004 無輸入驗證保護** | 🟡 中 | 只檢查 QUERY_UUID 是否為空，沒有對 UUID 字串長度或格式做驗證 |

---